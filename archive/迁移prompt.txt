我现在需要将此python应用整体迁移至nodejs编写 前后端分离，保留原软件内的全部功能 
先对原软件进行深入分析，不要遗漏任何功能或软件逻辑
对于扫码出入库功能做细化处理，增加待出库/入库的中间阶段，选择QSL分配卡号后由未出库/入库状态转变为待出库/入库的中间态，当扫码进行出入库操作时，再转变为已出库/入库状态
要求重构全部UI设计，需要符合web界面操作逻辑，预留完整的全功能api接口以便后续的手机app应用的功能对接，保留兼容导入python版本数据库，可直接将原先的数据库文件导入使用，导入后自动转换为新版数据库结构，原数据库中已经为已出库状态的卡片自动更新为最新的已出库状态，保留原有的出入库时间为最新版架构中的出入库时间
新建日志部分保留原代码中的全部内容，不要有任何删减，新建日志中增加支持连续日志记录，开启连续日志记录时，当当前日志保存完毕后保留除呼号外的全部信息等待下一条日志的保存
增设暗黑模式和白天模式的切换，默认为白天模式，可在代码中修改，而且需要记忆当前模式，不要出现切换模式后刷新页面又恢复切换前的模式的问题
地址标签打印可以针对单独的发信/收信地址进行多次打印
增设地址库存储，地址库数据库与QSL数据库独立，存储在代码根目录下，根据呼号对应存储地址信息以便后续打印地址标签自动填写，优化打印标签填写信息界面，可手动输入呼号或填入QSL编号进行查询对应呼号并自动从地址库中查找对应呼号的地址，发卡地址在第一次填入打印后自动保存，每次进行地址标签打印时自动填入
增设一个打印队列页面，QSL标签或地址标签打印时，打印的标签信息暂时不发送至pdf生成进行打印，而是缓存存储在打印队列中，需要在网页操作进行正式打印，可以批量选择进行打印
标签打印时要求直接唤起网页的打印请求而不是转换为pdf在网页中打开
日志管理自动根据时间顺序排列QSL日志信息，导入adif文件后自动对QSL信息进行序号排列修改，同时需要保证被修改编号的日志对应的QSL卡片入库/出库的信息(如有)绑定正确
代码根目录不再保存pdf文件，取消保存pdf文件的文件夹，转而使用代码根目录下temp临时文件夹缓存打印列表的打印数据，在打印完成后自动清空
要求整体代码模块化设计，以便于后续的代码维护工作
项目树结构参考如下 
-server.js 
-vite.config.js 
-package.json 
-index.html 
-src 
--main.jsx
--index.css 
--各个模块化组件
部署时，运行npm install，npm run build，node server.js运行完整项目，通过3000端口访问网页页面
整个项目不要使用任何需要预编译的库
在代码根目录下记录projects.log文件，用于整个项目的进度记录与同步